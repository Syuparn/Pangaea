# NOTE: cannot test multiple elements because
# order of map elements is not guaranteed.
assertEq(%{"a": 1}.until {|k, v| v.even?}.A, [["a", 1]])
assertEq(%{}.until {|k, v| v.odd?}.A, [])
# until returns iter (not arr)
assertEq(%{}.until {|k, v| v.odd?}.proto, Iter)
